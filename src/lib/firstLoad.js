export let firstLoad = true;

// function setFirstLoad() {

// 	// it should be set to true only after full page load the first time 
// 	// after that it return back to false on navigation
// 	if (document.readyState === 'complete') {
// 		firstLoad = true;
// 	} else {
// 		console.log('waiting for pageload');
// 	}

// }
