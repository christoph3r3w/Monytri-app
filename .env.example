# Appwrite config (required for client + server)
PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
PUBLIC_APPWRITE_PROJECT_ID=YOUR_PROJECT_ID

# Appwrite server API key (optional locally; required if your collections are not publicly readable)
APPWRITE_API_KEY=YOUR_SERVER_API_KEY
